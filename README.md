# fire

```
$ npm create cloudflare@latest -- my-hono-app --framework=hono
$ npm install npm-run-all --save-dev
$ npm run dev
$ npm run deploy
```

```
$ npx wrangler d1 create staff-directory
$ npx wrangler d1 execute staff-directory --command "SELECT name FROM sqlite_schema WHERE type ='table'"
$ npx wrangler d1 execute staff-directory --file=./schema.sql
$ npx wrangler d1 execute staff-directory --file=./schema.sql --remote
```
